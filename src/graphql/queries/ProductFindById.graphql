query ProductFindById($productId: ID!) {
  product(id: $productId) {
    id
    artist {
      name
    }
    category {
      name
    }
    coverImageUrl
    variants {
      name
      price
      stock
    }
    title
    releaseDate
    tracks {
      name
      number
    }
  }
}
