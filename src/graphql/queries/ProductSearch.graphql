query ProductsSearch($query: String!) {
  productSearch(query: $query) {
    id
    artist {
      name
    }
    title
    category {
      name
    }
    coverImageUrl
    variants {
      name
      price
      stock
    }
  }
}