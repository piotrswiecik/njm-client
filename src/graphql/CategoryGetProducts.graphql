query CategoryGetProducts($name: String!) {
  category(name: $name) {
    name
    products {
      id
      title
      artist
      category {
			  name
		  }
      collection {
			  name
		  }
      coverImg {
        width
        height
        url
      }
      price
    }
  }
}
