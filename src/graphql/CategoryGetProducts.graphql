query CategoryGetProducts($name: String!) {
  category(name: $name) {
    name
    products {
      id
      title
      artist
      coverImg {
        width
        height
        url
      }
    }
  }
}