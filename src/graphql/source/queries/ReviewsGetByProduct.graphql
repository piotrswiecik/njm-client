query ReviewsGetByProduct($productId: ID!) {
  productReviews(productId: $productId) {
    id
    headline
    content
    rating
    user {
      email
    }
    product {
      id
      title
    }
  }
}