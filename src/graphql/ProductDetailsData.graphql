query ProductDetailsData($productId: ID!) {
  product(id: $productId) {
    artist
    category {
      name
    }
    coverImg {
      height
      width
      url
    }
    id
    price
    releaseDate
    stock {
      qtyCd
      qtyLp
    }
    title
    tracks {
      name
    }
  }
}